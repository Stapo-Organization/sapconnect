#!/usr/bin/expect -f

set timeout 300
set ip "62.138.14.178"
set user "sapapimuntajat"
set password $env(DEPLOY_PASSWORD)

spawn ssh -o StrictHostKeyChecking=no $user@$ip "chmod +x install.sh && ./install.sh"
expect "*assword:*"
send "$password\r"
expect eof
